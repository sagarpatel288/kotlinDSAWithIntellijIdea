# Kotlin Basic Notes

## What is the difference between `Array<Long>` and `LongArray`? What is the default value of each?

* 

## What is the difference between `continue` and `break` in a `while loop` and a `for loop`?

* 