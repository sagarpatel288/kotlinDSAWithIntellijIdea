### What problem does a treap (randomized BST) solve?
* When do we use a treap (randomized BST)? Why?

### What are the pros and cons of a treap (randomized BST)?


### How does a treap (randomized BST) work?

* How do we implement a treap (randomized BST)?
* What underlying data structure do we use to implement a treap (randomized BST)?
* How do we perform various operations on a treap (randomized BST)?
* What is the time complexity of various operations on a treap (randomized BST)?
* What is the space complexity of various operations on a treap (randomized BST)?